export interface Theme {
  mainColor: string;
  secondaryColor: string;
  greyColor: string;
  bgColor: string;
  borderRadius: string;
}

export const theme: Theme = {
  mainColor: '#14213d',
  secondaryColor: '#fca311',
  greyColor: '#e5e5e5',
  bgColor: '#ffffff',
  borderRadius: '4px',
};
